class Mutations::Login < Mutations::BaseMutation
  def use_recaptcha?: () -> ::TrueClass

  def mutate: (username: untyped username, current_password: untyped current_password) -> { current_user: untyped }
end
