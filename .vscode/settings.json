{
  "emeraldwalk.runonsave": {
    "commands": [
      {
        "match": "\\.rb$|\\.rake$|Gemfile",
        "cmd": "docker exec -i music-server_web_1 bash -lc 'bundle exec rubocop -A `echo '${file}' | sed 's@.*/music-server/@@'`'"
      }
    ]
  }
}
