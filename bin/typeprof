#!/usr/bin/env
for file in lib/*.rb
do
  mkdir -p sig/$(dirname $file)
  rbs prototype rb $file > sig/${file}s
done

for file in lib/**/*.rb
do
  mkdir -p sig/$(dirname $file)
  rbs prototype rb $file > sig/${file}s
done
